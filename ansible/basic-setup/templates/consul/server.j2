#consul agent  -config-file=/etc/consul.d/server.hcl

data_dir = "/opt/consul"

client_addr = "{{ ansible_facts['eth1']['ipv4']['address'] }}" # host ip

ui_config{
 enabled = true
}

server = true


bind_addr = "{{ ansible_facts['eth1']['ipv4']['address'] }}" # host ip

bootstrap_expect=1

ports {
  grpc = 8502
}

connect {
  enabled = true
}